# Marka Çalışma Alanı (Brand Workspace)

Birden fazla markaya danışmanlık veren kişi için yerel bir Mac uygulaması. Tek cümlesi: **bir markaya bakınca ne yapıldı, ne bekliyor, müşteriye ne gidecek.** İş terminalde AI araçlarıyla (Claude Code, Codex CLI) yapılır; uygulama yapılanı gösterir, önerileri onaylatır ve doğrulanmış iş kayıtlarından kaynaklı müşteri raporu çıkarır.

Ürünün döngüsü: **Marka klasöründe AI ile çalışma → öneri → onay → doğrulanmış iş kaydı → müşteri raporu.**

> Beta 0.2.1. Swift 6, SwiftUI, GRDB (SQLite). macOS 14 ve üzeri, Apple Silicon (M1 ve sonrası).

Arayüz 0.2.1 sadeleştirmesinden ve ardından bağımsız arayüz denetiminin düzeltmelerinden (U9) geçti (`docs/surum-0.2.1.md`). Kabul ölçütlerinin ölçülmüş son hâli o belgenin sonundaki "Durum" bölümünde.

## Çalıştırma

```sh
scripts/build-app.sh                 # dist/Marka Çalışma Alanı.app (ad-hoc imzalı)
open "dist/Marka Çalışma Alanı.app"
scripts/test.sh                      # 133 test (Swift Testing)
python3 scripts/l10n.py check        # TR/EN çeviri ve İngilizce çoğul kapsamı
scripts/ui-olcum.sh                  # arayüz sadelik ölçümü (docs/surum-0.2.1.md §8)
scripts/release.sh                   # dist/MarkaCalismaAlani-<sürüm>.dmg (ad-hoc; katılımcı Gatekeeper uyarısı görür)
scripts/release.sh --sign "Developer ID Application: Ad Soyad (TAKIMID)" --notarize marka   # imzalı + notarize dmg
```

Sürüm numarasının tek kaynağı `Sources/MarkaCore/Version.swift`; Info.plist, dmg adı ve yedek manifesti oradan türer,
build numarası git commit sayısıdır (`scripts/version.sh`). Bu satırdaki "Beta X.Y.Z" bir testle aynı kaynağa bağlıdır.
Beta katılımcısı için kurulum: `docs/beta-kurulum.md`. Elle deneme: `docs/elle-test-senaryosu.md`.

Xcode gerekmez; Command Line Tools yeterli. Paket Xcode ile de açılabilir (`Package.swift`).

Geliştirme ve denetim için ortam değişkenleri:
- `MARKA_WORKSPACE=<klasör>`: veri tabanı konumu. Varsayılan `~/Library/Application Support/MarkaCalismaAlani`.
- `MARKA_FOLDERS=<klasör>`: marka klasörleri. Varsayılan `~/Documents/Marka Çalışma Alanı`.
- `MARKA_SNAPSHOT=<klasör>`: 14 ekranı (Bugün; Akış, ayrıntı paneli ve onaylanmış öneri paneli; Yapılacaklar ve paneli; Rapor; onay sayfası; iş kaydı düzenleyicisi; Bilgiler ve AI izinleri; Ayarlar › Genel ve Veri; ilk açılış; kenar çubuğunda yeni marka alanı) açık ve koyu görünümde PNG olarak çizer (28 dosya), sonra uygulamadan çıkar. Tercih yazmaz, Keychain'e dokunmaz; `MARKA_WORKSPACE`/`MARKA_FOLDERS` ile geçici veri alanında çalıştır (`swift run MarkaDogrula demo <klasör>` bekleyen ve onaylanmış öneriler dahil örnek veri kurar; onay sayfasının "Dosyalar" grubu için marka klasörüne elle bir dosya koy).
- `MARKA_TERMINAL_PROVA=1` (`MARKA_SNAPSHOT` ile birlikte): çizimden sonra gerçek `DetailView` ekran dışı bir pencerede sürülür ve terminal oturumunun yaşam döngüsü gerçek kabukla ölçülür (12 denetim, `TERMINAL PROVA: ✓/✗` satırları): gizleme, yana alma, başka markaya ve Bugün'e geçişte aynı süreç ve aynı görünüm; kabuk değişkeninin korunması (kabuk marka klasörüne yazar); yalıtım değişince oturumun sürmesi; arşivlemede yalnız o markanın, kapanışta tüm kabukların bitmesi. Kullanıcının zsh başlangıç dosyaları çalışmasın diye `SHELL=/bin/sh` ile çalıştır. Tıklama değil model durumu değiştirilir.

Doğrulama aracı:
```sh
swift run MarkaDogrula codex         # gerçek Codex App Server ile uçtan uca test (sentetik marka)
ANTHROPIC_API_KEY=sk-ant-… swift run MarkaDogrula anthropic [--model <kimlik>] [--max-tokens <n>]
                                     # gerçek Claude API ile 6 adım (sentetik iki marka), sonda token/maliyet tablosu
swift run MarkaDogrula demo <klasör> # sentetik örnek çalışma alanı
swift run MarkaDogrula joi <klasör>  # joi-todo verisiyle salt okunur kuru aktarım
```
`anthropic` komutu anahtarı yalnızca `ANTHROPIC_API_KEY` ortam değişkeninden okur (Keychain'e bakmaz); geçici klasörde
sentetik iki markalı çalışma alanı kurar ve sırayla anahtar doğrulama (ücretsiz token sayma), akışlı sohbet, araç döngüsü,
marka yalıtımı, bilgi derleme ve rapor özeti adımlarını `✓/✗`, süre, token ve tahmini maliyetle yazar (çekirdeği sınar;
bu yeteneklerin çoğunun 0.2.1'de arayüzü yok). Varsayılan model en ucuz uygun olan `claude-haiku-4-5-20251001`,
`max_tokens` üst sınırı 4096. Çıkış kodları: 0 geçti · 1 denetim düştü · 2 anahtar yok/argüman hatası · 3 anahtar
doğrulanamadı · 4 API hatası (kalan adımlar atlanır) · 5 marka yalıtımı delindi.
`ANTHROPIC_BASE_URL` verilirse istekler oraya gider (vekil ya da yerel sahte sunucu); o koşu gerçek API'yi sınamaz.

## Bölümler
- **Kenar çubuğu:** Bugün + markalar (her markada onay bekleyen sayısı — öneriler + klasördeki eklenmemiş dosyalar — vurgu renginde) + en altta "+ Marka". "+ Marka" (ya da ⇧⌘N) kenar çubuğunun altında tek bir alan açar: yer tutucu "Marka adı — ↩", sağında görünür *Ekle*; Enter da ekler, Esc vazgeçer; ayrı sayfa yok.
- **Bugün** (⌘0): tüm etkin markalarda (arşivlenenler hariç), boşsa tek satır sade metinle (her listede aynı boş durum biçimi): **Onay bekliyor** (marka · "3 onay bekliyor" · *İncele* → o markanın onay sayfası; sayı kenar çubuğuyla aynı), **Geciken** (marka · görev · kırmızı tarih), **Bu hafta yapılan** (marka başına "12 görev bitti · 11 iş kaydı · 2 dosya" ve "11 doğrulanmadı"), **Karar bekleniyor** (marka başına açık *Karar bekleniyor* başlıkları). Her satır tek düğmedir ve kaydın yerine götürür: marka açılır, ilgili bölüme geçilir, kayıt sağ panelde açılır. Tek arama burada (⌘F): sonuçlar bölümlerin yerine, marka · tür · başlık (görev, iş kaydı, söz/talep/karar başlığında; dosya/not başlık ve içeriğinde); satır kaydı aynı biçimde yerinde açar.
- **Marka ekranı:** başlıkta marka adı; sağda *Terminal* (açıkken seçili zeminli; ⌘J) ve *···* (Bilgiler ve AI izinleri…, Klasörü Finder'da göster, Dışa aktar…, Arşivle…). Onay bandı ve üç bölüm. Görsel dil: vurgu rengi yalnız birincil düğme, onay sayısı ve seçili sekme/satırda; tüm metin düğmeleri tek biçim (gövde rengi, üzerine gelince hafif zemin).
  - **Onay bandı ve onay sayfası (onayın tek yeri):** onay bekleyen varsa sekmelerin üstünde "N onay bekliyor · İncele". Sayfada tek görünür cümle ("Onayladıkların eklenir; Akış'tan geri alabilirsin."), gruplu satırlar: Yapılacaklar · İş kayıtları · Notlar · **Dosyalar** (marka klasöründe henüz eklenmemiş dosyalar, klasöre göre yolla) · Hafıza güncellemeleri. Satırlar varsayılan seçili; terminal önerisinin başlığı ve son tarihi düzeltilebilir. *Seçilenleri onayla (N)*: **yalnız seçilenler** onaylanır (dosya olduğu gibi saklanır); **seçilmeyenler bekler kalır**, bant sayısında görünmeye devam eder. *Tümünü reddet…* ayrı ve onaylıdır (önerileri reddeder; klasördeki dosyalar listede kalır). *Vazgeç* (Esc). Onaylanan her öneri Akış'ta "Öneri onaylandı" satırı olur ve oradan tek tek geri alınır.
  - **Akış** (⌘1) — ne yapıldı (onay istemez): günlere göre, en yeni üstte (son 200), tek satırlık öğeler: iş kaydı (doğrulanmamışsa sağda soluk "Doğrulanmadı"), biten görev ("Görev · Bitti"; iş kaydı bağlı olan görev yalnız iş kaydı satırıyla görünür), not, dosya, kapanan söz / karar bekleniyor / talep ("Söz · Bitti"), onaylanan öneri (hafıza güncellemesi dahil). Açık söz/talep/karar Akış'ta değil, Yapılacaklar'dadır. Satırda düğme yok: satıra tıklayınca sağda ayrıntı paneli; başlığında küçük *Kapat* (Esc, listenin boş yerine tıklama ve aynı satıra yeniden tıklama da kapatır). İş kaydında dolu sorular, bağlı dosyalar ve hemen altında *Düzenle* ile birincil *Doğrula* — **doğrulamanın tek yeri** (içerik görülerek; kural sağlanmıyorsa — "Ne yapıldı?" dolu ve en az bir dosya ya da görev bağlı — eksik orada yazar). *Düzenle* düz satırlı düzenleyiciyi açar (sorular, bağlı dosyalar, görev, tarih; doğrulanmış kayıt burada *Geri çek…*). Dosya/notta önizleme, *Arşivle*, *Aç*; onaylanan öneride *Geri al* (hafıza güncellemesi sayfanın önceki sürümüne döner). Üstte yalnız *Not ekle* (açıkken *Vazgeç*, Esc); dosya Akış'a sürüklenip bırakılır ya da marka klasörüne konur (klasördeki dosya onay sayfasına düşer). Boş Akış: "Henüz bir şey yok" + Claude'dan `oneriler/` klasörüne yazmasını isteyebileceğin ve dosya sürükleyebileceğin tek paragraf (bu akışın gerçek Claude oturumuyla denenmediği "Bilinen sınırlar"da).
  - **Yapılacaklar** (⌘2) — ne bekliyor: tek liste — açık görevler, söz, karar bekleniyor, açık talep; gecikenler üstte, sonra son tarihe göre, tarihsizler sonda. Boş sütun boş kalır. Satır başındaki kutu tamamlar (Bitti); satır 1,5 saniye üstü çizili kalıp çıkar. Üstte solda görünür tür seçimi "Görev | Söz", yanında alan ("Yeni görev — ↩"; Enter ekler). Satıra tıklayınca sağ panelde satır içi düzenleme (başlık, notlar, durum, son tarih, sorumlu; alanlar üzerine gelince hafif zemin; Enter ya da alandan çıkınca kaydeder) ve altta *Sil…* (onaylı) ile *İptal et* — tek yer (sağ tık menüsü kalktı). Hedef, teklif, sözleşme ve önemli tarih burada değil, Bilgiler'de. "Bitenler Akış'ta görünür." yalnız boş durumda.
  - **Rapor** (⌘3) — müşteriye ne gidecek: açılışta bu haftanın raporu, doğrulanmış iş kayıtlarından canlı önizleme (belge görünümü; boş bölüm gösterilmez, süre yoksa "Harcanan süre" yok). Raporun durumu *Taslak / Hazır* ("Onayla" fiili yalnız öneride). Başlık satırı tam genişlik: solda dönem menüsü (Bu hafta · Geçen hafta · Bu ay · Geçen ay), sağ kenarda *···* ve tek birincil düğme — taslakta **"Hazır, PDF al"** (raporu hazır işaretler ve filigransız PDF'i kaydeder; kaydetme panelinde vazgeçilirse taslak kalır), hazırken **"PDF"**. *···* menüsünde *Düzenle* ve *E-posta taslağı…* (yalnız hazır rapor; Mail'de taslak açar, göndermez). Düzenlerken giriş notu ve maddeler satır içi düzenlenir (metni silinen madde çıkar); markanın Claude/Codex izni varsa *AI ile özet* (özet varken aynı düğme *Özeti kaldır*), *Vazgeç* (Esc) ve birincil *Kaydet*. Rapor ilk PDF/e-posta anında ya da düzenlenince taslak sürüm olarak kaydedilir. Doğrulanmamış iş kayıtları, iş kaydı olmadan biten görevler ve (düzenlenmiş/hazır sürümde) sonradan değişen iş kayıtları önizlemenin üstünde **tek satırda** söylenir; eylemler aynı satırda (*Akış'ta gör*, *İş kayıtlarından yenile*). Maddeye tıklamak dayanağını Akış panelinde açar. Altta açılır **"Geçmiş" (tek yer)**: açık raporun sürümleri, paylaşımları ve markanın diğer kaydedilmiş raporları (tıklayınca açılır).
- **··· › Bilgiler ve AI izinleri:** tek sayfa; *Profil* (ad, sektör, tanım), *AI izinleri* (Claude, Codex; varsayılan kapalı; "Uygulama içindeki rapor özeti yalnız izin verdiğin sağlayıcıyı kullanır. Terminal bu izne bağlı değildir."), *Kişiler*, *Projeler* (alan adı "Amaç") ve varsa salt okunur *Hedef, teklif ve tarihler* (terminal önerisiyle ya da eski sürümde gelen hedef/teklif/sözleşme/önemli tarih; iptal edilenler hariç). Düzenlenebilir satır üzerine gelince hafif zemin alır; tıklayınca yerinde düzenlenir (Enter/Kaydet kaydeder, Esc vazgeçer); ayrı düzenleyici sayfası yok.
- **Terminal** (⌘J): marka klasöründe gerçek kabuk; başlıkta ad ve *Yana al / Alta al*. **Oturum marka başına yaşar:** terminali gizlemek (⌘J, başlıktaki *Terminal*), başka markaya ya da Bugün'e geçmek, paneli yana/alta almak ve yalıtım ayarını değiştirmek kabuğu (ve içinde çalışan Claude Code/Codex oturumunu) **sonlandırmaz**; geri dönünce aynı ekran ve aynı süreç görünür (SwiftTerm görünümü `AppModel`'deki önbellekten yeniden ebeveynlenir; `TerminalSessionCache`, `TerminalOturumTests`). Kabuk yalnız şu durumlarda biter: kullanıcı kabuktan çıkar (başlıkta "Kabuk kapandı." ve *Yeni oturum*), marka arşivlenir, uygulama kapanır — çalışan kabuk varsa kapanışta tek soru: "N terminalde çalışan oturum var; kapatılsın mı?". Sonlandırma kabuğun süreç grubuna SIGHUP, 2 saniye sonra hâlâ yaşıyorsa SIGKILL gönderir (etkileşimli kabuk SIGTERM'i yok sayar — ölçüldü). Marka yalıtımı açıkken (varsayılan; Ayarlar › Genel) `sandbox-exec` altında başlar: diğer markaların klasörleri ve uygulama verisi okunamaz, yazılamaz. Yalıtım ayarı değişince açık oturumlar başladıkları profille sürer, yeni açılan oturum yeni ayarla başlar; fark varsa terminal başlığında tek satırla söylenir. BAGLAM.md marka açılınca ve terminal her gösterildiğinde kendiliğinden yazılır.
- **Terminalden öneri** (`oneriler/`): terminaldeki araç (Claude Code, Codex CLI) veri tabanına yazamaz; yaptıklarını marka klasöründeki `oneriler/<tarih>-<konu>.json` dosyasına yazar (şema BAGLAM.md'de; CLAUDE.md/AGENTS.md oraya yönlendirir). Uygulama dosyayı marka ekranı açıkken 3 saniyede bir tarar; dosyanın tüm öğeleri tek işlemde bekleyen öneri olarak veri tabanına yazılır, dosya `oneriler/islenmis/` altına taşınır ve sha256'sı kaydedilir (aynı içerik ikinci kez önerilmez). Onay sayfasında yalnız bir kısmı onaylanan dosyanın kalan öğeleri veri tabanında bekler (dosya yeniden okunmaz; test: `kismenOnaylananDosyaninBekleyenleriKalirDosyaYenidenOnerilmez`). Görevler (yapılacak ya da bitti), iş kayıtları ("Doğrulanmadı" olarak; bağlı dosyalar eklenir), söz/talep/karar kayıtları ve notlar seçilip düzeltilerek onaylanır; hepsi Akış'tan geri alınabilir.
- **İlk açılış:** tek ekran — ilk markanın adı ve *Başla*. Eski görev listesinin içe aktarımı Ayarlar › Veri › İçe aktarım'da, bölümün içinde yerinde açılır.
- **Ayarlar** (⌘,), iki sekme: *Genel* — Claude API anahtarı (kaydet ve doğrula; kayıtlıysa kaldır), Codex girişi (tarayıcıda; durum kendiliğinden yenilenir; aynı düğme çıkış yapar), terminalde marka yalıtımı (değişiklik yeni oturumlarda geçerli). *Veri* — konum (Finder'da göster), yedekler (şimdi yedekle; her satırda hep görünen *Geri yükle…*), arşivlenmiş markalar (hep görünen *Arşivden çıkar*), eski görev listesini içe aktar, tanı bilgisini kopyala (beta ölçümleri dahil). Model ve düşünme derinliği seçilmez: rapor özeti sabit varsayılan Claude modeliyle ve varsayılan derinlikle çalışır, Codex kendi varsayılan modelini kullanır; eski sürümde kaydedilmiş seçim yok sayılır.
- **Menü ve kısayollar:** ⌘0 Bugün · ⌘1 Akış · ⌘2 Yapılacaklar · ⌘3 Rapor · ⌘J Terminal · ⌘F Ara · ⇧⌘N Yeni marka.
- Kaldırılan ekranların (Masa ve uygulama içi sohbet, Bilgi sekmesi, sayaç, Ayarlar › Kullanım, kayıt sayfası, marka logosu) verisi veri tabanında durur; çekirdek (bilgi derleme, sayaç kayıtları, oturumlar, denetim geçmişi) değişmedi.

## Güvence kuralları (kodda zorunlu)
- Ham dosya ve not içeriği SQL tetikleyicisiyle değiştirilemez; yalnızca arşivlenir.
- AI veri değiştirmez, öneri üretir. Öneriler kullanıcı onayıyla uygulanır ve geri alınabilir.
- Oturumun markası sonradan değişmez (SQL tetikleyicisi). Araçlar başka markanın kimliğini reddeder. İzin verilmeyen sağlayıcıya istek gitmez.
- Rapordaki "yapılan işler" yalnızca doğrulanmış iş kayıtlarından gelir. Her maddenin bir dayanağı olmalı. AI özet cümlesi geçerli madde referansı taşımıyorsa atılır.
- Her yazma işlemi denetim olayı bırakır. Günlük otomatik yedek alınır; geri yüklemeden önce mevcut veri yedeklenir.
- Terminal öneri dosyası (`oneriler/*.json`) yalnızca bulunduğu markaya öneri olur; dosyadaki marka adı/kimliği yok sayılır. Sembolik bağ, marka klasörü dışını gösteren yol, 256 KB'tan büyük dosya, 50'den fazla öğe ve şema dışı değer reddedilir; reddedilen dosyadan hiçbir öneri oluşmaz. Başka markanın görev/dosya kimliği reddedilir. Bağlı dosyaların kaydı yalnızca onayla oluşur.
- Tanı bilgisi (Ayarlar › Veri › *Tanı bilgisini kopyala*) yalnızca sürüm, macOS, mimari, dil, kayıt sayıları, AI bağlantı durumu, son 20 hatanın türü ve yeri (sabit anahtar) ile beta ölçümlerini taşır. Hata mesajı, marka adı, dosya/not metni, e-posta ve dosya yolu yazılmaz; test ayırt edici içerikle bunu doğrular. Son 200 hata çalışma alanındaki `tani-kayitlari.json` dosyasında durur ve hiçbir yere kendiliğinden gönderilmez.

## Belgeler
- `docs/surum-0.2.1.md`: 0.2.1 sadelik planı ve ölçülmüş durum (önceki: `docs/surum-0.2.0.md`)
- `docs/elle-test-senaryosu.md`: kurucunun 15-20 dakikalık elle deneme senaryosu
- `_bmad-output/planning-artifacts/`: PRD, mimari omurga, epikler (BMAD 6.12.0)
- `docs/eski-depo-analizi.md`: cli-todo-for-agentic README ile kod karşılaştırması ve aktarım
- `docs/dagitim-ve-saglayicilar.md`: App Store ile doğrudan dağıtım, yayın adımları, Anthropic ve Codex koşulları
- `docs/degerlendirme-plani.md`: 5 danışmanlı beta planı ve ölçümler
- `THIRD_PARTY_NOTICES.md`

## Bilinen sınırlar
- **Arayüz tıklanarak denenmedi.** 0.2.1 arayüzü ekran kilitliyken geliştirildi: ekranlar yalnız uygulama içi çizimle (`MARKA_SNAPSHOT`, açık/koyu) görüldü, çekirdek sorgu ve yazma yolları testli. Tıklama, klavye ve VoiceOver ile **denenmeyenler**: kenar çubuğu ve sekmeler, ⌘0–⌘3/⌘F/⌘J/⇧⌘N, onay sayfasının *Seçilenleri onayla*/*Tümünü reddet…*'i (seçilmeyenlerin beklemesi çekirdekte testli, arayüzde tıklanmadı), "Dosyalar" grubundan dosya ekleme, Akış'tan Geri al (hafıza güncellemesi dahil), ayrıntı panelinin *Kapat*, Esc ya da boş yere tıklamayla kapanması (panel açılınca odağı alır; Esc'nin her durumda ulaştığı doğrulanmadı), panelde Doğrula, satır içi düzenleme ve *Sil…*/*İptal et*, tamamla kutusunun 1,5 saniyelik bekleyişi, VoiceOver adlı eylemleri, not alanı, sürükle-bırak, kenar çubuğundaki yeni marka alanı (*Ekle*, Enter/Esc, odak kaybında kapanma), Bugün satırlarından ve rapor maddelerinden kaydın yerinde açılması, Rapor'daki *Hazır, PDF al* ile kaydetme paneli, Mail taslağı, düzenleme, *AI ile özet*, Geçmiş'teki diğer raporlar, Bilgiler'de satır içi düzenleme ve AI izinleri, metin düğmelerinin üzerine gelince aldığı zemin, Ayarlar'daki anahtar kaydı, Codex girişi (durumun 3 saniyede bir, en çok 3 dakika yenilenmesi dahil), yedek geri yükleme ve yerinde açılan içe aktarım. **Terminal oturumunun yaşaması** tıklanarak değil, ekran dışı bir pencerede gerçek `DetailView` ve gerçek kabukla (`/bin/sh`) model durumu değiştirilerek ölçüldü (`MARKA_TERMINAL_PROVA`, 12/12): gizleme, yana alma, B markasına ve Bugün'e geçişte süreç kimliği ve görünüm aynı kaldı, kabuk değişkeni korundu, arşivlemede yalnız o markanın ve kapanışta tüm kabuklar sonlandı. Kapanış sorusunun penceresi (NSAlert) ve kurucunun zsh'ı ile gerçek bir Claude Code oturumunun geçişte yaşaması **denenmedi**.
- Terminal oturumları yalnız uygulama açıkken yaşar: uygulama kapanınca (onayla) biter, yeniden açılışta kurtarılmaz. Aynı marka iki pencerede aynı anda terminali açık gösterilirse (Dosya › Yeni Pencere) tek terminal görünümü son çizilen pencereye taşınır, diğerinde boş kalır. Kapanıştaki "çalışan oturum" sayısı süren kabukları sayar; kabukta o an komut çalışıp çalışmadığına bakmaz.
- Onay bandı ve kenar çubuğu sayısı, klasördeki eklenmemiş dosyaları yalnız o markanın ekranı bu oturumda en az bir kez açıldıktan sonra (klasör arka planda tarandıktan sonra) içerir; hiç açılmamış markada yalnız bekleyen öneriler sayılır.
- Ekran çizimi pencereyi değil, aynı SwiftUI görünümlerini `ImageRenderer` ile ayrı ayrı çizer: pencere çerçevesi, araç çubuğu ve terminal çizilmez; açılır menü, giriş alanı ve tarih seçici AppKit denetimi olduğundan çizimde metin karşılıklarıyla görünür. Ayrıntı panelleri çizimde ilk değerle açık verilir. Bugün arama sonuçları çizilmez; iş kaydı düzenleyicisi (U9'da düz satırlara geçti) çizilir.
- AI'ın marka hakkında tuttuğu sayfalar arayüzde **"hafıza"** adını taşır (U9: "Bilgi güncellemesi" adı ··· › "Bilgiler" ile karışıyordu; "Marka notu" da Akış'taki "Not" ile karışacağı için seçilmedi; "hafıza" arayüzde başka hiçbir kavramda geçmez). Bilgi ekranı kalktığından hafıza sayfalarının içeriği arayüzde görünmez; yalnız onay sayfasında önerilen sürümün ilk 240 karakteri görünür. Onaylanmış bilgi güncellemesi Akış'tan önceki sürüme geri alınır (çekirdekteki `revertPage`: önceki içerikle yeni onaylı sürüm; geçmiş silinmez). **Geri alınamayanlar:** yeni sayfa açan güncelleme (önceki sürümü yok) ve sonrasında sayfaya başka sürüm onaylanmış güncelleme. Geri alınan bilgi güncellemesinin öneri kaydı "Onaylandı" durumunda kalır (çekirdek öneri durumunu değiştirmez); Akış satırı durur, panelde "geri alınamaz" açıklaması ve Geri al'ın yokluğu görünür. Önerisiz (eski sürümde doğrudan yazılmış) bilgi sürümleri Akış'ta görünmez.
- Değişiklik geçmişi (denetim olayları) 0.2.1'de arayüzde gösterilmez (kayıt sayfası kalktı); olaylar veri tabanında durur ve Tanı bilgisi sayılarına girer.
- İş kaydını doğrulamak yalnız ayrıntı panelinden (içerik görülerek) olur; Akış satırındaki tek tık *Doğrula* U9'da kalktı; doğrulayan adı Mac kullanıcısının tam adıdır (eski düzenleyicideki elle "Doğrulayan" alanı kalktı).
- Marka logosu arayüzden kalktı (hiçbir ekranda ve raporda gösterilmiyordu); kayıtlı logo veri tabanında durur.
- Akış en yeni 200 öğeyi gösterir; daha eskisi için sayfalama yok. Klasördeki yeni dosyalar marka ekranı açıkken her veri değişikliğinde arka planda yeniden taranır (dosyalar okunup özetlenir); çok büyük klasörde gecikebilir. Onay sayfasında dosya reddedilmez; istenmeyen yeni dosyayı listeden çıkarmanın yolu onu marka klasöründen taşımaktır ("yoksay" yok).
- Bugün araması görev/iş kaydı/söz-talep-karar kaydında yalnız başlığa bakar. Hafıza sayfası sonucu açılırsa yalnız marka açılır (sayfanın ekranı yok).
- Anthropic API gerçek bir anahtarla denenmedi; akış kayıtlı SSE yanıtlarıyla test edildi. Canlı doğrulama aracı hazır (`MarkaDogrula anthropic`), anahtar verilince çalıştırılacak.
- Codex App Server OpenAI tarafından deneysel olarak işaretli. **Marka yalıtımı:** her markanın Codex süreci ayrı başlar ve bizim macOS seatbelt profilimizle (`sandbox-exec`) sarılır. Diğer markaların klasörleri, uygulama veri alanı (veri tabanı, `Files`, `Yedekler`), kullanıcının `~/.codex`, `~/.claude` içeriği ve kabuk geçmişi (`~/.zsh_history`, `~/.zsh_sessions`, `~/.bash_history`) okunamaz, yazılamaz. Yazma yalnızca marka klasörüne, markanın Codex ev dizinine ve geçici klasörlere açıktır. Profil ayrıca profil dışı süreç başlatma yollarını da kapatır: LaunchServices/`open`, AppleEvents, Kısayollar (launchd/XPC ile başlatılan süreç profili miras almaz — ölçüldü: `open -g -j` ile başlatılan uygulama, kural eklenmeden başka markayı okuyup dışarı yazıyordu) ve `/private/tmp` · `/private/var/folders` altındaki yerel Unix soketleri (tmux/screen köprüsü — ölçüldü). Profil her başlatmada ölçülür (yasaklı bir sınama dosyası okunamamalı); ölçülemezse Codex başlatılmaz. `swift run MarkaDogrula codex` bunu gerçek Codex ile (codex-cli 0.146.0) kanıtlar. Markada Codex izni yine varsayılan olarak kapalıdır. Giriş/hesap/model listesi için kullanılan denetim süreci de aynı okuma yasaklı (marka klasörleri + veri alanı) profille sarılır; kullanıcının `codex` ikilisi kurcalanmış olsa bile marka verisi okunamaz. Bedelleri:
  - İç içe sandbox macOS'ta çalışmadığı için Codex'in kendi sandbox'ı kapalıdır (`danger-full-access`, onay politikası `never`). Marka klasörü dışına yazma onay sorulmadan **kesin redle** kapanır; onay kartı çıkmaz.
  - Kabuk komutlarının ağ erişimi (TCP) app-server'ınkinden ayrılamaz: Codex'in çalıştırdığı komutlar internete çıkabilir. `curl https` çalışır; yalnızca yerel Unix soketi köprüsü kapatıldı.
  - Okuma yasağı marka klasörleri, uygulama verisi, Codex/Claude ve kabuk geçmişiyle sınırlıdır. Bilgisayarın geri kalanı (`~/Desktop`, `~/Downloads` vb.) okunabilir; başka markanın dosyasını marka klasörü dışına kopyaladıysan Codex onu okuyabilir.
  - Sistem geçici klasörleri (`/private/tmp`, kullanıcının geçici klasörü) tüm markaların Codex süreçlerine ortaktır: bir markanın Codex'inin oraya yazdığı dosyayı başka markanınki okuyabilir. Codex'in kendi geçici klasörü (`TMPDIR`) markaya özeldir. (Bazı macOS araçları, ör. `mktemp`, `TMPDIR` yerine sistem geçici klasörünü kullanır; bu yüzden kapatılmadı.)
  - Her marka ayrı Codex ev dizini kullanır (`<veri alanı>/Codex/`). 0.1.0'da açılmış Codex oturumları devam ettirilemez; yeni Codex oturumu başlar, uygulamadaki sohbet geçmişi korunur. Kullanıcının `~/.codex/config.toml` ayarları (model, MCP sunucuları, proje listesi) uygulamanın Codex süreçlerine uygulanmaz. Giriş dosyası `auth.json` sembolik bağla paylaşılır; belirteç yenilemesinin bu bağ üzerinden yazıldığı **doğrulanmadı** (yenileme günler sonra olur).
  - Uygulama başka bir sandbox içinden açılırsa (iç içe) profil uygulanamaz ve Codex başlamaz.
- Marka terminali (varsayılan açık, Ayarlar › Genel'den kapatılabilir) diğer markaların klasörlerini ve uygulama verisini okuyamaz, yazamaz. Yazma kısıtı yoktur (kullanıcının kendi kabuğu), ancak köprü kurmayı önlemek için kabuk/araç yapılandırma dosyalarına yazma kapalıdır: `~/.zshenv`, `~/.zshrc`, `~/.zprofile`, `~/.zlogin`, `~/.zlogout`, `~/.zsh_sessions`, `~/.bash*`, `~/.profile`, `~/.config`, `~/.ssh`, `~/Library/LaunchAgents`, `~/.gitconfig`, `~/.claude.json`, `~/.claude`'daki yürütülebilir yapılandırma (`settings.json`, `settings.local.json`, `hooks`, `commands`, `agents`, `skills`, `plugins`, `CLAUDE.md`), `~/.codex/config.toml`, `~/.codex/packages`, `~/.local/bin`, `/usr/local/bin`, `/opt/homebrew/bin` (ölçüldü). Codex/Claude'un oturum ve geçmiş yazmaları ile `~/.zsh_history` açık kalır (yoksa kabuk/araç bozulur — ölçüldü). Terminal de Codex profiliyle aynı profil dışı süreç başlatma ve Unix soketi kapatmalarını taşır; `git push` (ssh, TCP 22) ve `ssh-agent` (launchd soketi) çalışmaya devam eder (ölçüldü). Bedelleri:
  - Terminalde kendi sandbox'ını kuran araçlar iç içe olduğu için komut çalıştıramaz. Ölçüldü: Codex CLI varsayılan kipte her komutu `sandbox-exec: sandbox_apply: Operation not permitted` ile düşürür; bu yüzden Codex CLI'yı yalıtımlı terminalde `codex --sandbox danger-full-access --ask-for-approval untrusted` ile başlatmak gerekir (sınırı terminal profili koyar, güvenli sayılmayan her komut onay ister). Komut elle yazılır; yalıtımlı terminal açılırken ekrana tek, soluk bir ipucu satırı olarak basılır (kabuğa gönderilmez, geçmişe ve dotfile'lara girmez; ekranda göründüğü **denenmedi**, açılışta ekranı temizleyen bir istem satırı silebilir). Yalıtımlı terminalde `claude --version`, `codex --version` ve `git` çalıştığı ölçüldü; tam Claude Code / Codex CLI oturumu (ve Claude Code'un isteğe bağlı sandbox'ı açıkken davranışı) **denenmedi**.
  - Terminal, yazamasa da okuyabilir: kullanıcının `~/.claude`, `~/.codex` içeriği ve `~/.zsh_history` gibi geçmiş dosyaları terminalden **okunabilir** (yalnızca Codex süreci bunları okuyamaz). Terminalde çalıştırılan Claude Code/Codex CLI kendi oturum kaydını yine `~/.claude`, `~/.codex` altında tutar; bu kayıtlar başka marka terminalinden okunabilir.
  - Sert bağ (hard link): yalıtımlı süreç başka markanın dosyasına sert bağ **kuramaz** (kaynak okunamadığı için `ln` reddedilir — ölçüldü). Ancak yalıtım dışında (elle) kurulmuş bir sert bağ marka klasörüne konursa içe aktarımda ayırt edilemez ve okunabilir; sembolik bağlar içe aktarımda atlanır/reddedilir.
  - Güvenlik tercihi "Marka yalıtımlı terminal" veri tabanında tutulur (profilde yasak olan veri alanı), böylece yalıtımlı Codex/terminal `defaults write` ile kapatamaz. macOS seatbelt'in `user-preference-write` kuralının `defaults` yazmasını **engellemediği** ölçüldü; bu yüzden korumanın kaynağı tercih değil veri tabanıdır.
- Marka yalıtımının kapatamadığı sınırlar (ölçülmüş): **varlık sızıntısı** — `test -e` / `cd` gibi işlemler yasaklı yolda `EPERM`, olmayan yolda `ENOENT` döndürdüğü için başka bir marka adının klasörünün *var olup olmadığı* çıkarılabilir (içeriği yine okunamaz); **pano** (`pbpaste`) yalıtım dışıdır, kullanıcının kopyaladığı içerik okunabilir; **ağ erişimi** açıktır (yalnızca yerel Unix soketi köprüsü kapatıldı), bir marka süreci internete veri gönderebilir; makinede Uzaktan Oturum Açma gibi yerel bir servis açıksa ona bağlanmak da engellenmez.
- Otomatik e-posta gönderimi yok. Planlı gönderim 0.2.1'de arayüzden kalktı ve **çalışmaz**: eski planlar veri tabanında durur, uygulama onları tetiklemez (saatlik denetim ve açılıştaki çalıştırma kapatıldı; çekirdekteki `DeliveryScheduler` testli ama çağrılmıyor).
- Rapor: bir sürümün "iş kayıtlarından kendiliğinden" oluştuğu (önizleme canlı kalır) sürüm notundan ("Kayıtlardan oluşturuldu") ve içerikten anlaşılır; arayüz dili değiştirilirse önceki dilde oluşmuş sürüm düzenlenmiş sayılır ve önizleme canlı içerik yerine o sürümü gösterir (değişen iş kaydı varsa "İş kayıtlarından yenile" satırıyla). PDF'teki bölüm başlıkları (ör. "Müşteri kararı bekleyenler") müşteriye giden belgenin parçası olarak çekirdekte kaldı; arayüzdeki ad "Karar bekleniyor".
- Terminal öneri köprüsü (İ7) testlerle ve gerçek `sandbox-exec` + terminal profiliyle doğrulandı (yalıtımlı kabuk `oneriler/`'e yazar, başka markanınkine yazamaz; çekirdek tarar, onayla görev oluşur). **Denenmedi:** gerçek bir Claude Code/Codex CLI oturumunun BAGLAM.md talimatına uyup JSON yazması (CLAUDE.md'deki `@BAGLAM.md` içe aktarımı dahil). Tarama anlık değil: marka ekranı açıkken 3 saniyede bir; son 1,5 saniyede değişen dosya bir sonraki taramaya kalır. Reddedilen öneriye bağlı dosyalar içerik adresli depoda kalır (dosya kaydı oluşmaz). "Bitti" diye önerilip tamamlanan var olan görev geri alınınca "yapılacak"a döner (önceki durumu "sürüyor" olsa da). Marka klasöründe elle kurulmuş sert bağ ayırt edilemez (içe aktarımla aynı sınır).
- Renk kontrastı (WCAG AA) macOS yüzeylerinin yaklaşık değerlerine (#FFFFFF/#ECECEC açık, #1E1E1E/#323232 koyu) göre hesaplanır ve testle bağlıdır; ekranda ölçülmedi. Koyu görünümde vurgulu düğme dolgusu beyaz yazıya ≥ 4.5:1 verir ama koyu pencere zeminine karşı 3:1'in altındadır (ikisi aynı anda sağlanamıyor).
- Deneme veri alanı (`MARKA_WORKSPACE`) kendi tercih takımını ve Keychain hesabını kullanır; AppKit'in paket kimliğine göre tuttuğu pencere konumu gibi görünüm durumu gerçek kopyayla ortaktır.
- İngilizce çoğul kuralları sayıyla isim çoğullayan 19 metnin hepsi için tanımlı (`Localizable.stringsdict`); testler Foundation çözümlemesiyle tekil/çoğul biçimi doğrular. `l10n.py check` yeni anahtarları sezgiyle yakalar (sayıdan sonra "-s" ile biten İngilizce kelime); düzensiz çoğullar (ör. "people") gözden kaçabilir. İngilizce arayüz ekran çizimiyle denetlenmedi.
- İlk açılış tek adıma indi (marka adı → bitti). Beta ölçümü `ilk_kurulum_dk` çekirdekte değişmedi: ilk açılıştan kullanıcının eklediği ilk dosya/nota kadar geçen süre. Artık not Akış'ta sonradan eklendiği için ölçü "kurulum süresi"nden çok "ilk kullanıma kadar geçen süre"yi gösterir. Terminal önerisiyle gelen notlar (AI kaynaklı) sayılmaz; yalnız terminalle çalışan kullanıcıda bu değer boş (`—`) kalabilir.
- Claude yalnızca API anahtarıyla, kullanım başına ücretli bağlanır; Claude.ai Pro/Max aboneliği bağlanamaz (Anthropic üçüncü taraf uygulamalarda buna izin vermiyor; `docs/dagitim-ve-saglayicilar.md`). 0.2.1'de Ayarlar'daki fiyat ve maliyet tahmini (Kullanım sekmesi) kalktı; kullanım kayıtları veri tabanında durur. Kayıtlı anahtar ayrıca yeniden doğrulanamaz; kaldırıp yeniden eklenir (eklerken doğrulanır).
- "Başka konuma yedekle…" kalktı; yedek klasörü Finder'dan kopyalanabilir, marka ··· › Dışa aktar ile ayrıca dışa aktarılabilir.
- Paket ad-hoc imzalı; başka Mac'te Gatekeeper uyarısı verir (`docs/beta-kurulum.md`). Developer ID imzası ve notarization `scripts/release.sh --sign … --notarize …` ile hazır ama hesap olmadığı için hiç çalıştırılmadı.
- Paket yalnızca Apple Silicon (arm64) içindir; Intel Mac'lerde açılmaz.
- Tanı kaydı hatanın türünü, vaka adını ve kodunu tutar; AI sağlayıcı hataları çoğunlukla `MarkaError.ai` olarak görünür, HTTP durum kodu ayrıca ayrıştırılmaz.

## Lisans
Tüm hakları saklıdır. Kaynak kodu inceleme amacıyla herkese açıktır; kopyalama, değiştirme ve dağıtım için izin verilmez. Üçüncü taraf bileşenler: `THIRD_PARTY_NOTICES.md`. Geliştirmede kullanılan BMAD Method (MIT) depoya dahil değildir; `npx bmad-method install` ile ayrıca kurulur.
